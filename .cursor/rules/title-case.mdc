---
description: Title case for labels/headings; sentence case for descriptions
alwaysApply: true
---

# User-facing copy case

## Title case — labels and chrome

Use **title case** (capitalize the first letter of each word) for short UI chrome:

- Button, label, heading, menu, and tab copy
- Empty-state **titles**, toast/feedback titles, and short status labels
- Form field labels

Examples:

- ✅ `Forgot Password`
- ✅ `Send Reset Link`
- ✅ `Your Email Is Not Verified`
- ❌ `Forgot password`
- ❌ `Send reset link`

**Code:** toast/feedback messages go through `notify.*()` in `lib/frontend/feedback/notify.ts`, which applies `capitalizeFeedbackText()` from `lib/frontend/feedback/format.ts`. Author short chrome strings in title case at the source.

## Sentence case — descriptions and supporting copy

Use **sentence case** for longer supporting text so it stays readable:

- Page/section **subtitles** and **descriptions**
- Helper text under headings, hero lead paragraphs, and bullet supporting lines
- In-form success/body paragraphs (not button labels)

Examples:

- ✅ `Enter your account email and we'll send reset instructions if the address is registered.`
- ✅ `Log in to your workspace. Your Crawllex dashboard is ready.`
- ❌ `Enter Your Account Email And We'll Send Reset Instructions…`

Match an external API message verbatim when displaying it as-is.
